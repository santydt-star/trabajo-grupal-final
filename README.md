# Trabajo final 
## Estudiantes: 
- **Dubreucq, Agustin**
* **Escalada, Romina**
+ **Taboada, Santiago** 

## Algoritmos agregados:
### sort_bubble:
El método burbuja `(Bubble Sort)` consiste en comparar a los elementos adyacentes de la lista e intercambiarlos si se encuentran en una posición incorrecta. De este modo, los “ítems” de mayor valor se desplazan hacia el final, generando un orden de menor a mayor.
 #### Imprementacion:
Se implemento ya que aparte de ser obligatio, es el algoritmo de imprementacion mas sencillo de hacer y entender, aunque no sea el algoritmo mas estable y rapido  
### sort_selection:
El método de Selección `(Selection Sort)` recorre la lista para tomar el valor más pequeño de la parte no ordenada, cuando lo encuentra lo intercambia con el elemento que se encuentra al inicio de la sección, organizando el algoritmo de menor a mayor repitiendo este proceso.
 #### Implementacion:
Este algoritmo es implementado al ser el algoritmo mas sencillo despues del `bubble sort` y en este podemos aprender a dividir en fases el algoritmo, incitandonos a estudiar y buscar nueva informacion de los algoritmos.  
### sort_insertion: 
 El método de Inserción `(Insertion sort)` arma la lista ordenada de forma progresiva. En cada paso, toma un elemento de la parte no ordenada y lo desplaza hacia la izquierda hasta encontrar su lugar correspondiente en la lista. Este proceso se repite para cada elemento, recorriendo toda la lista. Insertando en la posición correcta obtiene un orden de menor a mayor.  
 #### Implementacion:
Fue implementado al ser un algoritmo de ordenamiento que actua rapido, y tiene una mayor complejidad que los anteriores algoritmos de oredenamientos, por eso nos permite aprender mejor sobre la escritura de los codigos.
## Dificultades encontrada: 
- 1. La principal dificultad encontrada para todos los miembros del equipo fue el que no era necesario usar ciclos, sino solo usar condiciones ya que eran mini pasos en el `step()`
* 2. Al principio fue complicado entender github, pero una vez que se comprendio fue mucho mas facil 
